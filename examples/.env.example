# OpenAI-compatible endpoint shared by host + every agent (overrideable per-agent in topic.md).
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-replace-me
HOST_MODEL=gpt-4o

# Optional: dedicated model for the puzzle visual director (one call per
# puzzle, plans surface + conclusion image beats). Defaults to HOST_MODEL.
# A stronger model here improves story-beat ordering and frame variety.
SCENE_PLANNER_MODEL=

# Separate compression model. Used to summarise per-agent memory.md when it grows past a token threshold.
# Endpoint/key default to OPENAI_BASE_URL/OPENAI_API_KEY when blank.
COMPRESSION_BASE_URL=
COMPRESSION_API_KEY=
COMPRESSION_MODEL=gpt-4o-mini

# Podcast Q&A / global chat + semantic search. QA_MODEL defaults to HOST_MODEL.
# EMBEDDING_MODEL vectorizes transcripts/sources through the same
# OpenAI-compatible endpoint; leaving it empty disables semantic search and the
# Q&A entry points. Both models are overrideable from the admin App Config.
# EMBEDDING_DIMENSIONS fixes the stored vector width (pgvector column) —
# changing it later requires dropping discussion_chunks and re-indexing.
QA_MODEL=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536

# Azure Cognitive Services Speech (TTS).
AZURE_SPEECH_KEY=replace-me
AZURE_SPEECH_REGION=eastus

# Cost accounting for the per-run "total cost" figure. These fold the non-LLM
# API spend into the total alongside the LLM token cost. Defaults shown; set to
# 0 to exclude a source from the total.
#  - Azure neural TTS: $15/1M chars (prebuilt neural PAYG; use 22 for Neural HD).
#  - Lyria 3 Pro: ~$0.08 per billed music generation (cache hits aren't billed).
AZURE_TTS_COST_PER_MILLION_CHARS=15
LYRIA_COST_PER_GENERATION=0.08

# Points purchase fulfillment and admin product setup.
REVENUECAT_WEBHOOK_AUTH=
REVENUECAT_REST_API_KEY=
REVENUECAT_PROJECT_ID=
REVENUECAT_APP_ID=
REVENUECAT_API_BASE_URL=
APP_STORE_CONNECT_ISSUER_ID=
APP_STORE_CONNECT_KEY_ID=
APP_STORE_CONNECT_PRIVATE_KEY_BASE64=
APP_STORE_CONNECT_APP_ID=
APP_STORE_CONNECT_API_BASE_URL=

# Where audio segments, the combined debate.mp3, transcript.txt, memory/, and run.log are written.
OUT_DIR=./out

# Optional S3-compatible storage for finished media. Cloudflare R2 uses
# S3_REGION=auto and S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com.
S3_BUCKET=
S3_REGION=
S3_ENDPOINT=
S3_PREFIX=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
# Optional public/custom download domain. When set, download URLs use this
# base URL instead of presigned S3 URLs.
S3_DOWNLOAD_BASE_URL=
